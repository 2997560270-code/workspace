# Product Drill MVP 测试先行开发计划

## 1. 项目目标

Product Drill 的 MVP 目标是做出一个可真实使用的 Web 端 AI 产品思维训练工具，帮助销售、售前、客户成功、创业者和 B2B/AI+/企业培训相关从业者，通过模拟客户对话、需求澄清、方案表达和自有产品分析，训练完整的产品思维。

第一版重点验证三件事：

- AI 对话训练是否能帮助用户更快理解客户痛点。
- AI 评分是否能指出用户在需求澄清、方案设计和价值论证中的具体短板。
- 自有产品分析是否能帮助用户重新理解自己的产品，并获得后续优化方向。

## 2. 技术栈

- 前端与后端：Next.js + TypeScript。
- UI：Tailwind CSS。
- 数据库：PostgreSQL。
- ORM：Prisma。
- 登录：Auth.js。
- AI：OpenAI-compatible API。
- 测试：Vitest + Playwright。
- 部署：Vercel + 托管 PostgreSQL。

## 3. 开发原则

- 每个模块独立闭环开发，先写测试，再开发功能。
- 一个模块通过测试、e2e 和人工审核后，才进入下一个模块。
- 不提前做课程、支付、社区、文件解析、团队后台等非 MVP 能力。
- 尽量复用当前 demo 的灰白、简洁、严肃 B2B 工具风格。
- 图表先用轻量 CSS/SVG 实现，不引入重型图表库。
- 文档、demo 和阶段产物同步到桌面 workspace 与飞书 workspace。

## 4. 模块循环流程

每个模块固定执行以下流程：

1. 写该模块的最小测试。
2. 运行测试，确认测试先失败。
3. 开发该模块的最小可用功能。
4. 运行模块测试，确认通过。
5. 跑对应 e2e 流程。
6. 提供页面或结果给人工审核。
7. 审核通过后同步到桌面 workspace 和飞书 workspace。
8. 进入下一个模块。

## 5. 模块开发顺序

### 5.1 项目骨架与页面框架

目标：把当前 demo 的结构迁移成真实 Next.js 应用。

测试：

- 首页能打开。
- 左侧导航能切换工作台、我的产品、对话历史、能力画像、场景库。
- 页面标题和说明随导航变化。

开发：

- 初始化 Next.js、TypeScript、Tailwind。
- 搭建统一布局。
- 迁移当前 demo 的灰白、简洁、严肃风格。

e2e：

- 打开首页。
- 依次点击五个导航。
- 确认每个页面内容正确展示。

人工审核：

- 确认整体视觉风格、导航结构和页面布局没有偏离当前 demo。

### 5.2 场景库与训练配置

目标：支持选择训练场景、训练模式、难度和训练角色。

测试：

- 默认场景为 AI+。
- 场景只包含 B2B、AI+、企业员工培训。
- 训练模式包含用户需求提出和客户咨询。
- 页面不再出现使用场景字段。

开发：

- 建立场景配置。
- 工作台读取配置渲染表单。
- 保留用户需求提出与客户咨询的说明。

e2e：

- 选择不同场景。
- 切换用户需求提出和客户咨询。
- 确认训练配置不会丢失。

人工审核：

- 确认训练配置逻辑清楚，字段没有冗余。

### 5.3 登录与数据库基础

目标：让训练记录、消息、评分、产品资料可以归属到用户。

测试：

- 未登录用户不能进入主工作台。
- 登录后能创建用户记录。
- 数据库能保存用户基础信息。

开发：

- 接入 Auth.js。
- 接入 Prisma + PostgreSQL。
- 建立 User、TrainingSession、Message 三个基础模型。

e2e：

- 未登录访问工作台时跳转登录。
- 登录后进入工作台。
- 创建一条训练记录。

人工审核：

- 确认登录门槛和 MVP 使用方式可以接受。

### 5.4 AI 对话训练

目标：用户能创建一次训练，并与 AI 对话。

测试：

- 创建训练后生成一条 AI 开场问题。
- 用户发送消息后，AI 返回下一轮追问。
- 消息保存到数据库。
- 至少支持 3 轮对话。

开发：

- 封装 AI 服务层。
- 实现用户需求提出和客户咨询两种提示词。
- 实现消息保存和页面实时展示。

e2e：

- 创建用户需求提出训练。
- 完成 3 轮对话。
- 创建客户咨询训练。
- 完成 3 轮对话。

人工审核：

- 用一个真实场景对话，确认 AI 不泛泛聊天。

### 5.5 方案提交与评分

目标：用户提交方案后，AI 输出评分和改进建议。

测试：

- 提交方案后生成总分。
- 输出 7 个维度分。
- 至少指出 3 个具体问题。
- 评分结果能保存并回看。

开发：

- 建立 Evaluation 数据模型。
- 固定评分维度：需求理解、问题澄清、方案设计、异议应对、商业价值论证、数据与逻辑、表达与沟通。
- 评分输出使用结构化 JSON。

e2e：

- 完成一轮训练。
- 提交产品方案。
- 查看评分结果。
- 从历史页回看评分。

人工审核：

- 确认评分足够严格、具体、有训练价值。

### 5.6 我的产品分析

目标：用户录入自己的产品资料，AI 分析并追问。

测试：

- 用户能保存产品名称、介绍、目标用户、核心功能、阶段、产品链接。
- AI 能复述产品理解。
- AI 至少提出 5 个围绕真实问题的追问。
- AI 输出不少于 3 条优化建议。

开发：

- 建立 ProductProfile 和 ProductAnalysis。
- 实现产品资料表单。
- 实现 AI 产品理解摘要、追问和优化建议。
- 优化建议分为短期可做、中期验证、长期方向。

e2e：

- 录入一个产品资料。
- 生成产品理解摘要。
- 回答 AI 追问。
- 查看优化建议。

人工审核：

- 用真实产品样例验证 AI 是否准确理解产品。

### 5.7 对话历史

目标：用户能查看历史训练和评分结果。

测试：

- 历史页能列出训练记录。
- 能按训练模式和场景展示。
- 点击记录能查看对话和评分。
- 空状态显示清楚。

开发：

- 实现历史列表。
- 实现训练详情页。
- 复用消息和评分数据。

e2e：

- 完成一轮训练。
- 进入对话历史。
- 打开训练详情。
- 查看对话和评分。

人工审核：

- 确认历史信息足够复盘，不增加多余筛选器。

### 5.8 能力画像

目标：用训练数据生成能力趋势和下一步训练建议。

测试：

- 显示平均分、完成训练数、最高分、综合进步。
- 显示最近训练表现趋势。
- 显示能力维度表现。
- 显示高频短板和下一步推荐训练。

开发：

- 从 Evaluation 聚合能力数据。
- 复刻当前 demo 中的能力画像结构。
- 图表先用 CSS/SVG 简单实现。

e2e：

- 完成多次训练。
- 进入能力画像。
- 查看趋势、维度表现、高频短板和下一步推荐。

人工审核：

- 确认画像能指导下一轮训练。

### 5.9 场景库完善

目标：让场景库成为训练入口，而不是静态展示。

测试：

- 三个场景均可查看说明。
- 每个场景能一键进入训练。
- 进入训练时自动带入场景。

开发：

- 场景库连接训练创建接口。
- 保持场景内容轻量，不做知识库。

e2e：

- 从 B2B 场景进入训练。
- 从 AI+ 场景进入训练。
- 从企业员工培训场景进入训练。

人工审核：

- 确认三个 MVP 场景够用。

### 5.10 同步与总验收

目标：保证本地和飞书都有最新产物。

测试：

- 桌面 workspace 中存在最新项目文件。
- 飞书 workspace 中存在最新 demo、文档和阶段源码包。
- 本地 e2e 全部通过。

开发：

- 每轮审核通过后同步。
- 源码按阶段压缩为 product-drill-app.zip 上传飞书。
- 文档和 demo 继续覆盖对应飞书文件。

e2e：

- 跑完整 MVP 主流程。
- 验证本地文件与飞书文件均可访问。

人工审核：

- 确认本地和飞书文件一致。

## 6. 测试策略

单元测试：

- 场景配置。
- AI 输出解析。
- 评分聚合。
- 能力画像计算。
- 高频短板和下一步推荐训练。

接口测试：

- 创建训练。
- 发送消息。
- 提交方案并评分。
- 保存产品资料。
- 生成产品分析。

E2E 测试：

- 工作台训练。
- 客户咨询。
- 我的产品分析。
- 历史回看。
- 能力画像。

人工审核：

- 每个模块独立验收。
- 未通过人工审核时，不进入下一个模块。

## 7. 同步规则

- 本地开发目录：C:\Users\A\Desktop\workspace\product-drill-app。
- 开发计划文档、demo、产品文档、路线图文档同步到 C:\Users\A\Desktop\workspace。
- 飞书同步目标为云文档 workspace 文件夹。
- 阶段源码用 product-drill-app.zip 上传飞书，避免源码文件结构在云盘中散乱。
- 如果飞书 workspace 被共享者修改，先从飞书下载对比，再同步回本地桌面 workspace。

## 8. MVP 不做范围

- 不做课程内容。
- 不做支付。
- 不做社区。
- 不做图片、PDF、Word 等文件解析。
- 不做团队管理。
- 不做复杂企业后台。
- 不做多产品档案管理，第一版只保存一个当前产品档案。
- 不做 RAG 知识库，第一版用结构化场景配置和提示词。

## 9. 验收标准

- 用户能完成用户需求提出训练闭环。
- 用户能完成客户咨询训练闭环。
- 用户能完成自有产品分析闭环。
- AI 能提出至少 5 个围绕真实问题的追问。
- AI 评分能指出至少 3 个具体问题。
- AI 产品优化建议不少于 3 条，并分为短期、中期、长期。
- 用户能在对话历史中回看训练记录。
- 用户能在能力画像中看到趋势、维度表现、高频短板和下一步推荐训练。
- 本地桌面 workspace 与飞书 workspace 均有最新产物。
