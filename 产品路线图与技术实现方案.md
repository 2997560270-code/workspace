# 产品路线图与技术实现方案

## 1. 产品路线图

### 1.1 MVP 目标

MVP 目标是验证“AI 对话训练能否帮助销售/售前/客户成功和创业者提升产品理解、需求澄清和产品表达能力”。第一版不做课程、不做社区、不做支付、不做复杂企业后台。

第一版主路径保留两条，但都做轻：

- 需求训练路径：AI 扮演用户提出需求，使用者追问、澄清、提交方案，AI 给出严厉评估。
- 自有产品路径：使用者输入产品介绍和链接，AI 分析产品、连续追问，并给出产品定位优化建议。

首批场景只做 3 个：

- B2B
- AI+
- 企业员工培训

### 1.2 阶段 0：原型验证

周期：1-2 周。

目标：确认训练闭环是否成立。

交付：

- 训练工作台低保真原型。
- 我的产品分析页低保真原型。
- B2B、AI+、企业员工培训各 1 个场景样本。
- 用户需求提出提示词。
- 自有产品分析提示词。
- 评分提示词。

验收：

- 用户能在 10 分钟内完成一轮训练。
- AI 能提出具体追问，而不是泛泛聊天。
- AI 评估能指出至少 3 个具体问题。

### 1.3 阶段 1：MVP 开发

周期：4-6 周。

目标：做出可真实使用的 Web 应用。

交付：

- 邮箱登录。
- 新建训练。
- 对话训练。
- 方案提交。
- AI 结构化评分。
- 训练历史。
- 基础能力画像。
- 自有产品资料填写。
- 产品理解摘要。
- 产品追问训练。
- 产品优化建议。

验收：

- 用户能完成“AI 提需求 -> 用户追问 -> 用户提交方案 -> AI 评分”的闭环。
- 用户能完成“上传产品资料 -> AI 理解摘要 -> AI 追问 -> AI 优化建议”的闭环。
- 系统能保存训练记录、消息、评分和一个当前产品档案。

### 1.4 阶段 2：小范围试用

周期：2-4 周。

目标：验证目标用户是否愿意持续使用。

交付：

- 20-50 名目标用户试用。
- 训练完成率统计。
- 二次训练率统计。
- 高频短板统计。
- 场景和提示词修订。

验收：

- 至少 50% 试用用户完成 1 轮训练。
- 至少 30% 试用用户完成第 2 轮训练。
- 用户反馈里能明确说出“哪里帮我想清楚了”。

### 1.5 阶段 3：商业化前增强

周期：4-8 周。

目标：为后续咨询或课程内容转化做准备，但产品内仍不做完整课程。

可加功能：

- 导出训练报告。
- 多个产品档案。
- 更多行业场景。
- 团队训练数据看板。
- 咨询预约入口。

暂不加：

- 完整课程体系。
- 用户社区。
- 文件解析上传。
- 自定义模型训练。

## 2. 技术选型

### 2.1 推荐技术栈

| 模块 | 选择 | 原因 |
| --- | --- | --- |
| 前端/后端 | Next.js + TypeScript | 一个项目同时承载页面和 API，MVP 开发最快。 |
| UI | Tailwind CSS | 少写样式文件，适合快速做工作台界面。 |
| 数据库 | PostgreSQL | 训练记录、消息、评分、产品档案都是结构化数据。 |
| ORM | Prisma | 类型安全，迁移清晰，适合 Next.js 全栈项目。 |
| 登录 | Auth.js | 先做邮箱登录，后续可扩第三方登录。 |
| AI | OpenAI Responses API 或兼容 OpenAI 接口的大模型服务 | 支持对话、结构化输出和后续模型替换。 |
| 部署 | Vercel + 托管 PostgreSQL | 和 Next.js 配合最省事，适合 MVP。 |
| 测试 | Vitest + Playwright | Vitest 测核心逻辑，Playwright 测主流程。 |

### 2.2 选型依据

- Next.js 官方定位是用于构建全栈 React Web 应用，适合本项目这种页面、API、服务端逻辑都不复杂的 MVP。
- Prisma 负责数据库访问和迁移，避免手写大量 SQL。
- Auth.js 负责认证，先只做邮箱登录，减少账号体系成本。
- OpenAI Responses API 或兼容接口负责 AI 对话、评分和产品分析；所有 AI 调用封装在服务层，后续可换模型。
- Vercel 负责部署，减少服务器运维。

参考：

- Next.js Docs: https://nextjs.org/docs
- Prisma Docs: https://www.prisma.io/docs/orm
- Auth.js Docs: https://authjs.dev/getting-started
- Vercel Next.js Docs: https://vercel.com/docs/frameworks/full-stack/nextjs
- OpenAI Responses API: https://platform.openai.com/docs/api-reference/responses

### 2.3 不选的方案

- 不选独立 React + Node 后端：当前团队和需求没必要拆两个项目。
- 不选低代码：AI 对话、评分、历史记录和产品档案会很快碰到定制边界。
- 不做 RAG 知识库：MVP 只有 3 个场景，先用结构化场景配置和提示词即可。
- 不做文件解析：MVP 只支持文本和链接。

## 3. 系统架构

### 3.1 模块划分

- Web UI：训练工作台、我的产品分析页、评分页、历史页、能力画像页。
- API 层：训练、消息、评分、产品档案、AI 调用。
- AI 服务层：统一封装用户模拟、客户质询、方案评分、自有产品分析。
- 数据层：用户、训练会话、消息、评分、行业场景、产品档案。
- 场景配置：B2B、AI+、企业员工培训的角色、痛点、流程、约束和指标。

### 3.2 核心数据表

User：

- id
- email
- name
- createdAt

TrainingSession：

- id
- userId
- industry
- mode
- role
- difficulty
- status
- createdAt
- completedAt

Message：

- id
- sessionId
- sender
- content
- createdAt

Evaluation：

- id
- sessionId
- totalScore
- dimensionScores
- strengths
- weaknesses
- missedQuestions
- improvementAdvice
- createdAt

ProductProfile：

- id
- userId
- productName
- productDescription
- targetUsers
- coreFeatures
- productStage
- productUrl
- createdAt
- updatedAt

IndustryScenario：

- id
- industry
- roles
- painPoints
- workflows
- constraints
- metrics

### 3.3 AI 输出格式

所有 AI 结果都要求结构化 JSON，再渲染成页面内容。

方案评分输出：

```json
{
  "totalScore": 78,
  "dimensionScores": {
    "painInsight": 15,
    "userUnderstanding": 12,
    "requirementClarification": 13,
    "solutionFit": 16,
    "feasibility": 8,
    "metrics": 7,
    "structure": 7
  },
  "strengths": ["能识别主要使用者"],
  "weaknesses": ["没有说明替代方案"],
  "missedQuestions": ["当前流程是谁负责执行？"],
  "improvementAdvice": ["先补清楚用户每天如何完成这个任务。"]
}
```

自有产品分析输出：

```json
{
  "productSummary": "这是一个面向 B2B 销售团队的客户需求训练工具。",
  "questions": [
    "目标用户最常输在哪个销售环节？",
    "用户现在不用你的产品时如何训练新人？"
  ],
  "familiarityScore": 72,
  "optimizationAdvice": {
    "shortTerm": ["把目标用户从销售团队细分到售前新人。"],
    "midTerm": ["验证训练后是否提升客户访谈质量。"],
    "longTerm": ["沉淀企业内训场景和团队能力报告。"]
  }
}
```

## 4. 实现方案

### 4.1 代码结构

```text
app/
  page.tsx
  dashboard/page.tsx
  training/[id]/page.tsx
  product/page.tsx
  history/page.tsx
  profile/page.tsx
  api/
    training/route.ts
    training/[id]/message/route.ts
    training/[id]/evaluate/route.ts
    product/route.ts
    product/analyze/route.ts
lib/
  auth.ts
  db.ts
  ai.ts
  prompts/
    scenarios.ts
    demand-training.ts
    product-analysis.ts
    evaluation.ts
prisma/
  schema.prisma
tests/
  ai-output.test.ts
  training-flow.test.ts
  product-flow.spec.ts
```

### 4.2 开发任务

#### Task 1：项目初始化

- 创建 Next.js + TypeScript 项目。
- 接入 Tailwind CSS。
- 配置 ESLint。
- 添加基础布局：左侧导航、主内容区。

验收：

- 首页能打开。
- 导航能进入训练、我的产品、历史、画像页面。

#### Task 2：数据库和认证

- 配置 PostgreSQL。
- 添加 Prisma。
- 建立 User、TrainingSession、Message、Evaluation、ProductProfile、IndustryScenario。
- 接入 Auth.js 邮箱登录。

验收：

- 用户能登录。
- 登录用户能创建自己的训练记录。
- 未登录用户不能访问工作台。

#### Task 3：行业场景配置

- 写入 B2B、AI+、企业员工培训 3 个场景。
- 每个场景包含角色、流程、痛点、约束、指标。
- 场景先用代码配置，不做后台编辑。

验收：

- 新建训练时能选择 3 个场景。
- AI 提示词能读取对应场景。

#### Task 4：训练工作台

- 创建训练会话。
- 展示对话消息。
- 支持用户发送追问。
- AI 按用户需求提出模式回复。
- 支持提交方案。

验收：

- 用户能完成至少 3 轮对话。
- 消息能保存到数据库。
- 用户能提交方案。

#### Task 5：AI 评分

- 根据评分维度生成结构化 JSON。
- 保存评分结果。
- 展示总分、分项分、优点、盲点、漏问问题、改进建议。

验收：

- 每次评分都有总分和 7 个维度分。
- 反馈至少包含 3 个具体问题。
- 评分结果能在历史页再次查看。

#### Task 6：我的产品分析

- 用户填写产品名称、介绍、目标用户、核心功能、当前阶段、产品链接。
- 只保存一个当前产品档案。
- AI 先生成产品理解摘要。
- 用户确认后进入追问。
- AI 输出熟悉度评分和优化建议。

验收：

- 用户能保存产品资料。
- AI 能准确复述产品定位、目标用户和核心功能。
- AI 至少提出 5 个追问。
- AI 至少输出 3 条优化建议。

#### Task 7：历史和能力画像

- 历史页展示训练日期、行业、模式、得分、主要短板。
- 能力画像页展示平均分、短板次数、最近建议训练方向。
- 只做基础统计。

验收：

- 用户能看到自己的历史记录。
- 平均分和高频短板能随训练更新。

#### Task 8：试用数据和上线

- 增加基础事件记录：开始训练、提交方案、完成评分、完成产品分析。
- 部署到 Vercel。
- 准备 5 个试用账号。

验收：

- 线上环境可访问。
- 试用用户能完整跑通两条主路径。

## 5. 测试方案

### 5.1 单元测试

- AI JSON 解析失败时返回可读错误。
- 评分维度缺失时不保存 Evaluation。
- ProductProfile 只保留用户当前一个档案。

### 5.2 集成测试

- 创建训练 -> 发送消息 -> 保存消息。
- 提交方案 -> 调用评分 -> 保存 Evaluation。
- 保存产品资料 -> 分析产品 -> 生成追问和建议。

### 5.3 端到端测试

- 登录后完成一轮需求训练。
- 登录后完成一轮自有产品分析。
- 历史页能看到训练记录和得分。

## 6. 验收清单

- 支持 B2B、AI+、企业员工培训 3 个场景。
- 支持用户需求提出模式和自有产品分析模式。
- 支持严厉型评分反馈。
- MVP 不包含课程、支付、社区、文件解析。
- 自有产品只支持文本资料和链接。
- 产品档案 MVP 只保存一个当前档案。
- 商业化入口只保留后期咨询或课程内容方向，不在 MVP 中实现。

