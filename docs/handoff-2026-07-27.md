# Product Drill AI 交接文档（2026-07-27）

> AI 辅助生成。后续工作只允许在主项目目录 `G:\codex_project\no.2_project\product_drill_AI` 中进行。

## 1. 现在做什么

当前没有进行中的代码修改。仓库已完成清理并回到干净的 `master`：

- `HEAD`、`master`、`origin/master`、`origin/HEAD` 均为 `fa429295dd2855e5f4f688143734cccdddbdd47e`。
- 主项目是唯一 Git worktree；stash 为空；工作区无未提交内容。
- 当前产品代码只在 `product-drill-app/`。仓库入口见 [`README.md`](../README.md)，项目背景见 [`CONTEXT.md`](../CONTEXT.md)。

下一会话的工作重点是继续核查 GitHub Issues：逐项按验收条件、现行代码和测试证据判断真实完成度，再更新状态；不要仅凭提交信息或文件时间判断。

## 2. 已经完成了什么

### 仓库与本地目录整理

- PR [#21](https://github.com/2997560270-code/workspace/pull/21) 已合并，合并提交为 [`fa42929`](https://github.com/2997560270-code/workspace/commit/fa429295dd2855e5f4f688143734cccdddbdd47e)。
- 删除了旧的 `product-drill-mvp/` 重复应用、Demo V2/V3 路由与启动器、重复计划和历史验收记录；共删除 143 个过时文件、19,811 行。
- 新增简明仓库入口 [`README.md`](../README.md)，并忽略 `.playwright-mcp/`、`audit-evidence/` 等本地产物。
- 四个项目临时 worktree、旧合并快照、Playwright 临时证据、截图和过时 stash 已清除。
- 已合并的相关前端 PR：[#18](https://github.com/2997560270-code/workspace/pull/18)、[#19](https://github.com/2997560270-code/workspace/pull/19)、[#20](https://github.com/2997560270-code/workspace/pull/20)。

### 验证结果

- TypeScript 类型检查通过。
- Vitest：28 个测试文件、238 个测试通过。
- 黄金评测：30 个案例校验有效、31 个测试通过。
- Next.js 生产构建通过。
- 清理后现行 Playwright 测试：15/15 通过。
- `npm audit --omit=dev --audit-level=high`：0 个漏洞。
- PR #21 的两组 `quality` 与 `browser` GitHub CI 全部通过。

### Issues 当前入口

Issues 以 GitHub 为唯一事实来源，操作规范见 [`docs/agents/issue-tracker.md`](./agents/issue-tracker.md)，标签规范见 [`docs/agents/triage-labels.md`](./agents/triage-labels.md)。当前开放集合为：

- Epic：[#1](https://github.com/2997560270-code/workspace/issues/1)
- `ready-for-agent`：[#4](https://github.com/2997560270-code/workspace/issues/4)、[#5](https://github.com/2997560270-code/workspace/issues/5)、[#6](https://github.com/2997560270-code/workspace/issues/6)、[#7](https://github.com/2997560270-code/workspace/issues/7)、[#12](https://github.com/2997560270-code/workspace/issues/12)、[#13](https://github.com/2997560270-code/workspace/issues/13)、[#14](https://github.com/2997560270-code/workspace/issues/14)
- `ready-for-human`：[#8](https://github.com/2997560270-code/workspace/issues/8)、[#15](https://github.com/2997560270-code/workspace/issues/15)

开放不等于完全未实现；这些 Issue 需要按正文验收条件重新取证后才能关闭或改标签。

## 3. 卡在哪里

当前没有技术阻塞。

尚未完成的是开放 Issues 的第二轮逐项验收：部分需求已有实现和测试，但仍需将 Issue 正文中的每条验收条件映射到具体代码、测试及运行结果。`ready-for-human` 的 #8、#15 涉及产品定义/业务判断，不能由代理自行假设结论。

GitHub 的 Git 传输曾短暂无法连接 `github.com:443`。最终通过 GitHub API 获取并验证签名提交对象后，已将本地引用安全同步到 `fa42929`；这不是当前阻塞。

## 4. 下一步做什么

1. 从 GitHub 读取 #4、#5、#6、#7、#12、#13、#14 的完整正文和评论，先建立“验收条件 → 代码/测试证据 → 结论”的核查表。
2. 建议按依赖顺序核查：#7 → #13 → #5 → #4 → #12 → #6 → #14；只补真实缺口，不重复已有实现。
3. 对确已满足全部验收条件的 Issue，附证据评论后关闭；部分满足的保留开放并精确更新标签/剩余项。
4. #8、#15 只整理需要人类确认的决策点，等待确认后再改状态。
5. 子 Issue 全部验收后再处理 Epic #1。
6. 每次修改都在主项目目录中新建 `codex/` 分支，验证后提交、推送、开 PR；合并后切回并同步 `master`。

## 5. 哪些坑不要踩

- 不要创建临时 worktree，不要把代码、交接稿或审计证据保存到 `C:\tmp`、AppData Temp 或其他临时目录。
- 不要恢复已删除的 `product-drill-mvp/`、`demov2/`、Demo V3 页面、旧启动脚本和历史重复文档；`product-drill-app/` 是唯一活动应用。
- 不要把 GitHub 文件列表的“最后修改时间”当成使用状态。它表示最后触碰该路径的提交时间，也可能保留原作者时间。
- 不要仅根据 Issue 标题、提交消息或“代码看起来存在”就关闭 Issue；必须逐条验证验收条件和可运行测试。
- 不要替人类决定 #8、#15 的产品主张或禁止推断边界。
- 仓库存在 `.codegraph/`；理解或定位代码时先用 `codegraph.cmd explore`，再用 `rg` 补充搜索。
- PowerShell 执行策略会拦截 `npm.ps1` 和 `codegraph.ps1`；使用 `npm.cmd`、`codegraph.cmd`。
- 不要强推、重写 `master` 或批量删除不明确路径。GitHub 网络异常时先核对远端状态，不要用 force 操作“修复”引用。
- 保留 `.github/`、`docs/`、`plugins/product-drill-lifecycle/`、`marketplace.json`、`AGENTS.md`、`CONTEXT.md`、两份现行产品方案和 `product-drill-app/`。

## Suggested skills

- `github:github`：读取和更新 Issues/PR 元数据，核对仓库状态。
- `code-review`：按 Issue 验收条件审查现有实现与测试证据。
- `diagnose`：仅在测试失败或实现行为与验收条件不符时使用。
- `github:yeet`：完成实际修复后提交、推送并创建草稿 PR。
